def Remove(tuples):
    while () in tuples:
        tuples.remove(())
        return tuples

tuples = [(), ('Ram', '15', '8'), (), ('Srinivas', 'Lakshmi'), ('Amar', 'Akbar', '45'), ('', ''), ()]
print(Remove(tuples))