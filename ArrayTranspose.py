import numpy as np
twoDArray = np.array([[1, 2, 3], [4, 5, 6]])
print("The transpose Array is:", twoDArray.transpose)
print("Shape of Transpose Array is:", (twoDArray.transpose()).shape)
print("The reshape of an Array is:", reshape(6, 1))

sortedArray = twoDArray.sort()
print("The sorted array is:", sortedArray)